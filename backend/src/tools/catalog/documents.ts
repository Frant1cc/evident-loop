// Phase 1: Unified document generation
// Old generate_word_document tool has been removed and replaced with start_document_generation
// which is registered dynamically in app.ts with the artifact application

export const documentToolModules: never[] = [];
